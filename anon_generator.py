import pandas as pd
import redis

 # Create Database + Local Cache... pull from Database to Local Cache each Run... check Cache first then check Database
        # Create Schema
    # Write Design Doc

 # Check Types Heavilly (or have each function typecast)

def empi_anon(empi):
    anon_empi = '00000000'
    return anon_empi

    # Types to Anon
        # EMPI -->
        # Accession #1 & #2 --> 
    
    # Numeric Type (Input --> Alphanumeric | Output --> Entirely Numeric)

    # UID --> Can't have leading 0

    # Anonymization occurs in MedEncrypter.py
        # Anon.py wrapper that calls function onto different tags